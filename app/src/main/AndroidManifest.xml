<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.codefordenver.encorelink">

    <application
        android:name=".FirebaseAppOnStartConfiguration"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateOrganizerProfile" />
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".OrganizerDashboard" />
        <activity android:name=".CreateMusicianProfile" />
        <activity
            android:name=".EditOrganizerProfile"
            android:theme="@style/AppTheme.SlidrActivityTheme" />
        <activity android:name=".TestMusicianDashboard" />
    </application>

</manifest>
